<section class="hero">
  <div class="inner">

    <div class="eyebrow">Self-experiment in progress &nbsp;·&nbsp; Vol. 01</div>

    <h1 class="title-stack">
      <span class="word word-smaller">Smaller</span>
      <span class="word word-stronger">Stronger</span>
      <span class="word word-shredded">Shredded</span>
    </h1>

    <div class="hero-copy">
      <p class="tagline">One body. Relentless variables. No shortcuts.</p>
      <p class="descriptor">
        Training logs, hard lessons, and the <em>skill stacks that make life feel palpable</em> —
        documented by a tree surgeon, coder, and dad
        twenty years into the question.
      </p>
    </div>
  </div>  

    <div class="hero-signup">
  <p class="signup-eyebrow">Follow the experiment in real time</p>
  <form
    class="signup-form"
    action="YOUR_KIT_FORM_ACTION_URL"
    method="post"
    target="_blank"
  >
    <input
      class="signup-input"
      type="email"
      name="email_address"
      placeholder="your@email.com"
      required
      autocomplete="email"
    />
    <button class="signup-btn" type="submit">
      Join the experiment
    </button>
  </form>
  <p class="signup-trust">
    No pitch. No filler. Just the log — sent when something worth saying happens.
  </p>
</div>
    

<style>
    /* ── HERO ── */
    .hero {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      /* padding: 0 2.5rem; */
      padding-top: 58px;
      max-width: 1160px;
      margin: 0 auto;
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      gap: 2.75rem;
    }

    .inner {
      align-self: self-end;
    }

    .eyebrow {
      font-size: 0.68rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--color-text-muted);
      margin: 1rem 0 2.8rem 0;
      display: flex;
      align-items: center;
      gap: 1rem;
    }
    .eyebrow::before {
      content: '';
      width: 2.2rem;
      height: 1px;
      background: var(--color-accent);
      flex-shrink: 0;
    }

    /* ── TITLE STACK ── */
    .title-stack {
      display: flex;
      flex-direction: column;
      line-height: 0.88;
      margin-bottom: 3.2rem;
      user-select: none;
    }

    .word {
      font-family: 'Fraunces', serif;
      display: block;
    }

    .word-smaller {
      font-size: clamp(2rem, 7.5vw, 7.5rem);
      color: var(--color-text-muted);
      letter-spacing: 0.07em;
      font-variation-settings: 'opsz' 144, 'wght' 100;
    }

    .word-stronger {
      font-size: clamp(4rem, 10vw, 9rem);
      color: var(--color-text);
      letter-spacing: -0.015em;
      font-variation-settings: 'opsz' 144, 'wght' 900;
    }

    .word-shredded {
      font-size: clamp(2rem, 7.5vw, 7.5rem);
      font-style: italic;
      color: var(--color-accent);
      letter-spacing: 0.04em;
      font-variation-settings: 'opsz' 144, 'wght' 500;
    }

    /* ── COPY ── */
    .hero-copy {
      max-width: 560px;
      margin-bottom: 3.5rem;
    }

    .tagline {
      font-size: clamp(1rem, 2.2vw, 1.3rem);
      font-weight: 300;
      color: var(--color-text);
      line-height: 1.45;
      margin-bottom: 1rem;
    }

    .descriptor {
      font-size: 0.875rem;
      color: var(--color-text-muted);
      line-height: 1.8;
    }
    .descriptor em {
      color: var(--color-text-mid);
      font-style: normal;
    }

    /* ── EMAIL SIGNUP ── */
.hero-signup {
  max-width: 520px;
  margin-bottom: 3.2rem;
  align-self: self-end;
}

.signup-eyebrow {
  font-size: 0.68rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--color-text-muted);
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
  gap: 0.8rem;
}

.signup-eyebrow::before {
  content: '';
  width: 1.6rem;
  height: 1px;
  background: var(--color-accent);
  flex-shrink: 0;
}

.signup-form {
  display: flex;
  gap: 0;
  margin-bottom: 0.75rem;
}

.signup-input {
  flex: 1;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-right: none;
  color: var(--color-text);
  font-family: var(--font-sans);
  font-size: 0.875rem;
  padding: 0.75rem 1rem;
  border-radius: var(--radius-sm) 0 0 var(--radius-sm);
  outline: none;
  transition: border-color var(--transition);
  min-width: 0;
}

.signup-input::placeholder {
  color: var(--color-text-faint);
}

.signup-input:focus {
  border-color: var(--color-accent);
}

.signup-btn {
  background: var(--color-accent);
  color: var(--color-bg);
  border: 1px solid var(--color-accent);
  font-family: var(--font-sans);
  font-size: 0.8rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  padding: 0.75rem 1.25rem;
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  cursor: pointer;
  white-space: nowrap;
  transition: background var(--transition), color var(--transition);
}

.signup-btn:hover {
  background: transparent;
  color: var(--color-accent);
}

.signup-trust {
  font-size: 0.72rem;
  color: var(--color-text-faint);
  line-height: 1.6;
}

/* ── RESPONSIVE ── */
@media (max-width: 480px) {
  .signup-form {
    flex-direction: column;
    gap: 0.5rem;
  }

  .signup-input {
    border-right: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
  }

  .signup-btn {
    border-radius: var(--radius-sm);
    text-align: center;
    padding: 0.85rem;
  }
}

    

    /* ── RESPONSIVE ── */
    @media (max-width: 640px) {
      .hero {display: block}
      .nav-links { gap: 1.2rem; }
      .nav-links li:not(:last-child):not(:nth-last-child(2)) { display: none; }
      .hero { padding: 0 1.25rem; padding-top: 58px; }
      .log-stats { gap: 1.5rem; }
      .scroll-cue { display: none; }
    }
  </style>